<Page x:Class="sUpdater.InfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:sUpdater"
      mc:Ignorable="d" 
      d:DesignHeight="435" d:DesignWidth="800" Loaded="Page_Loaded">
    <RichTextBox x:Name="infoTextBox" IsReadOnly="True" IsDocumentEnabled="True" VerticalScrollBarVisibility="Auto">
        <RichTextBox.Document>
            <FlowDocument>
                <FlowDocument.Blocks>
                    <Paragraph>
                        Loading...
                    </Paragraph>
                </FlowDocument.Blocks>
            </FlowDocument>
        </RichTextBox.Document>
        <RichTextBox.Resources>
            <!-- Remove the margin between the lines -->
            <Style TargetType="{x:Type Paragraph}">
                <Setter Property="Margin" Value="0" />
            </Style>
        </RichTextBox.Resources>
    </RichTextBox>
</Page>
